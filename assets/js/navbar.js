$(document).ready(() => {
    $('#nav-home').on('click', () => ShowTab('home'));
    
    $('#nav-news').on('click', () => ShowTab('news'));
    
    $('#nav-settings').on('click', () => ShowTab('settings'));
});